{
  "name": "bookmarks",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "license": "UNLICENSED",
  "author": "xdrdak",
  "bin": {
    "bookmarks": "./src/bin.ts"
  },
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/bin.ts",
    "lint": "oxlint --import-plugin -D no-commonjs && npm run lint:no-param-props",
    "lint:no-param-props": "test -z \"$(grep -rnE 'constructor\\([^)]*\\b(readonly|private|public|protected)\\s+\\w+\\s*:' src/ || true)\"",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "test": "vitest"
  },
  "dependencies": {
    "citty": "^0.2.1"
  },
  "devDependencies": {
    "@types/node": "^25.0.6",
    "oxfmt": "^0.23.0",
    "oxlint": "^1.38.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
